#executando o cucumber e gerando report no site  https://reports.cucumber.io/reports 
#  default: --publish
bdd: --format pretty
json: --format=message 
stand-alone: --json-formatter
default: --publish-quiet

